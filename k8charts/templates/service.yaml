apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8charts.fullname" . }}
  labels:
    {{- include "k8charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8charts.selectorLabels" . | nindent 4 }}
